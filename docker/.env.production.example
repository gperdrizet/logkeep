# Production environment template
# Copy to .env.production and fill in real values.

# ----------------------------------------------------------------------------
# Core app settings
# ----------------------------------------------------------------------------
ENVIRONMENT=production
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO
ADMIN_USERNAMES=admin

# ----------------------------------------------------------------------------
# Security
# ----------------------------------------------------------------------------
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your-session-secret-here

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-key-here

# ----------------------------------------------------------------------------
# Database
# ----------------------------------------------------------------------------
POSTGRES_USER=logkeep_admin
POSTGRES_PASSWORD=your-postgres-password-here
POSTGRES_DB=logkeep
DATABASE_URL=postgresql://logkeep_admin:your-postgres-password-here@postgres:5432/logkeep

# ----------------------------------------------------------------------------
# Email / SMTP
# ----------------------------------------------------------------------------
SMTP_HOST=smtp.ionos.com
SMTP_PORT=465
SMTP_USER=your-email@perdrizet.org
SMTP_PASSWORD=your-smtp-password-here
SMTP_FROM=your-email@perdrizet.org

# ----------------------------------------------------------------------------
# LLM API (OpenAI-compatible)
# ----------------------------------------------------------------------------
LLM_BASE_URL=https://your-openai-compatible-endpoint/v1
LLM_API_KEY=your-llm-api-key-here
LLM_MODEL_NAME=your-model-name

# ----------------------------------------------------------------------------
# Operational limits
# ----------------------------------------------------------------------------
MAX_TAGS_PER_USER=1000
MAX_RETRIES=3
PROCESSING_TIMEOUT_MINUTES=5
REQUEST_TIMEOUT=10
ACCESS_TOKEN_EXPIRE_MINUTES=10080
ALGORITHM=HS256